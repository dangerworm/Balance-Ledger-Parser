# Ledger HTR Lab Frontend

React + Vite single-page app for quickly testing HTR on uploaded ledger images.

## Commands

```bash
npm install
npm run dev
```

Set `VITE_API_URL` to point at the backend if it is not running on `http://localhost:8000`.
